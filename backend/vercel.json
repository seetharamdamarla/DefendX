{
    "buildCommand": "pip install -r requirements.txt --break-system-packages && export PATH=\"$PATH:$HOME/.local/bin\" && prisma generate",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/app.py"
        }
    ]
}