{
    "builds": [
        {
            "src": "app.py",
            "use": "@vercel/python",
            "config": {
                "buildCommand": "pip install -r requirements.txt --break-system-packages && python -m prisma generate"
            }
        }
    ],
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/app.py"
        }
    ]
}